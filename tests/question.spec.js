import {
    mount
} from 'vue-test-utils';
import expect from 'expect'

import question from '../src/components/Question'

describe('question', () => {
    let wrapper;
    beforeEach(() => {
        wrapper = mount(question)
    })
})